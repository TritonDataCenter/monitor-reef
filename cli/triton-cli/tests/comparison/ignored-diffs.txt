# Intentional divergences between Node.js and Rust triton CLI
#
# Format: test-id  reason
# Lines starting with # are comments.
#
# These tests are skipped entirely. Use this for differences that are
# by design and will never be "fixed." For differences that can be
# normalized away (e.g., stripping repo URLs), add normalization to
# the script instead — no entry needed here.

# Top-level help uses clap's format which is fundamentally different
# from node-triton's grouped layout. This is a deliberate choice.
help                clap vs node-triton help layout is by design

# --version includes different repo URLs; version numbers are already
# normalized but the repo URL line differs. This is expected.
version             different repo URLs is intentional

# Fish shell support: Node.js doesn't support fish (exits 1),
# Rust adds fish support (exits 0). This is an intentional improvement.
env-fish            Rust adds fish shell support that Node.js lacks

# Bash completion: completely different systems (node-dashdash 2K lines
# vs clap_complete 13K lines). clap_complete is the standard for clap CLIs.
completion-bash     clap_complete vs node-dashdash is by design

# Volume refs field: VOLAPI always returns "refs": [] but Node.js triton
# strips it from output. Rust matches the real API response.
volume-list-json    Rust matches VOLAPI response; Node strips refs field
volume-get          Rust matches VOLAPI response; Node strips refs field
volume-get-json     Rust matches VOLAPI response; Node strips refs field
