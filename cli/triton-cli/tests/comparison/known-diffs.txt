# Known diffs between Node.js and Rust triton CLI
#
# Format: test-id  bead-id  [description]
# Lines starting with # are comments.
#
# When a diff is fixed and the test flips to PASS, the script flags it
# as "fixed?" so you know to close the bead and remove the line.

# Profile commands — differences.md #6, #7, #8
profile-get         monitor-reef-cws  profile get exits 1 with no output
profile-get-json    monitor-reef-cws  profile get -j exits 1 with no output
profile-list-json   monitor-reef-610  profile list -j shows insecure instead of curr

# Env command — differences.md #9
env-bash            monitor-reef-bxv  double vs single quotes in exports

# Missing alias — discovered by help coverage normalization
# (not a help-* test; tracked separately since it's a real feature gap)
# help-image          monitor-reef-7wk  image copy missing 'cp' alias
