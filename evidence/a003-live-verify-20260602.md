# A003 modular split — live verify on 192.168.1.182 (2026-06-02)

Closes `beads-oxq0`. Validates the A003 modular split
(`crates/mantas3/s3/src/s3_impl/` split into `ops/` + `shared/`)
together with all downstream Phase 1/2/3 surface-s3 work on a
real FDB-backed mantad + tritond deploy.

## Build

- **manta-storage commit**: `fe7302998da6a0060d300330db05b6ccf56060b3`
  (surface-s3, includes CHG-193 public-bucket-name uniqueness gate)
- **monitor-reef commit**: `f1a4e70c05b5eed9c493d21dee457728c52daf43`
  (surface-s3)
- **Host target**: `x86_64-unknown-illumos`
- **Built at**: 2026-06-02T23:14:39Z

Build host(s): build01 (mantad/mantad-adm/tritond, sequential),
build02 (tcadm). Mantad release build envelope:
`RUSTUP_TOOLCHAIN=stable`/`1.92`, `CARGO_INCREMENTAL=0`,
`RUSTFLAGS="-L /home/build/opt/fdb/lib"`,
`LIBCLANG_PATH=/opt/local/lib`. tcadm needed an FDB-libclang-free
build envelope (no `--features foundationdb`).

Bundle: `/tmp/phase-c-bundle-fe73029.tar.gz` (63 MB). Repacked with
`tar --format=ustar` because Mac's BSD tar emits pax-extended headers
that illumos tar rejects under `set -eu` in `phase-c-deploy.sh`.

## Deploy

`phase-c-deploy.sh` on `192.168.1.182` (host `build00`,
SmartOS GZ): tritond + mantad backed up to `.prev`, new
binaries installed at `/opt/{mantad,tritond}/bin/`, admin
token + root SigV4 creds reused from prior deploy. Result:

- `mantad pid=43290` (`--meta-plane=fdb`, admin-token-gated,
  SigV4 enabled, listening on `:7443`)
- `tritond pid=43292` (`/etc/tritond/config.toml`, listening on
  `:8080`)

tcadm refresh token was expired (last login 2026-06-01). Recovered
via `tritond reset-root-password` → fresh credentials → `tcadm
login --username root --password-stdin`.

## Verify results

### 1. `tools/s3-data-plane-gate-verify.py` — ✅ 0 failures

- Workspace A `t-aa72d499516e4ff181aeb1d33ae94ca2` and
  Workspace B `t-233236a0cb7e4f9eb4392308ed15823e` provisioned.
- Each workspace minted one IAM user (`alice-a`, `alice-b`)
  and created one bucket via SigV4 (`phase1-a-096d7b29`,
  `phase1-b-fac5ff88`).
- **12/12 cross-tenant probes returned `NoSuchBucket`**:
  alice-a's head/list/put/get/delete/multipart/copy/delete_bucket
  against alice-b's bucket; alice-b's symmetric set against
  alice-a's bucket.
- `list_buckets` visibility: each caller sees only her own
  bucket — no cross-tenant leak.
- Same-workspace happy path: alice-a `put_object` +
  `get_object` round-trips intact.

### 2. `tools/s3-presigner-cache-fallback-verify.py` — ✅ 0 failures

- **Item 3 — cache eviction on `drop_silo_tenant_storage`**:
  workspace dropped → workspace archived on mantad → workspace
  rebound → new pre-drop vs post-rebind AKID assertion. Result:
  `MKIAFVUAMXOMPBUATAFX → MKIALH45DMMNOC7BQYZV` (different
  AKIDs, cache evicted, fresh fetch on rebind). n4w7 cascade
  workaround engaged (deleted `presigner-t-…` bucket pre-drop).
- **Item 4 — cluster-root presigner fallback**: admin-URL AKID
  matches `AKIA97EF32849F791C1D` (the cluster-root AKID) —
  Unscoped fallback path engaged for fleet-admin presigns.

### 3. `tools/s3-presigner-tritond-verify.py` — ✅ 0 failures

- Two tenants provisioned in silo `988ae554-3508-44cc-afb8-a2300fbeaf13`
  (`96e5614b-…`, `de7c1fa2-…`).
- Each tenant minted an operator account, logged in, and
  created a bucket via the **tritond admin proxy**
  (`HTTP 201`): `presign-tritond-a-1ba8fe`,
  `presign-tritond-b-693a45`.
- **In-workspace presign PUT/GET**: AKID prefix
  `MKIAXYTNZWGYCHWQ…`. PUT → 200, GET → 200 with correct body.
- **Cross-workspace presign**: alice-a's URL against
  alice-b's bucket → **404 NoSuchBucket** (workspace gate
  fires on mantad). Symmetric B→A → **404 NoSuchBucket**.

## Coverage

This run validates:

- **A003 modular split** (`crates/mantas3/s3/src/s3_impl/`
  split into `ops/` + `shared/`) — primary structural check.
- **Phase 1 workspace gate** — cross-tenant probes blocked at
  the data plane.
- **Phase 2 presigner credentials** — per-workspace cache
  + eviction on rebind + cluster-root Unscoped fallback.
- **Phase 3 tritond-mediated presigner** — full proxy chain
  (admin-backend → tritond → mantad presigner cache).
- Implicit: every C-phase port that landed on surface-s3
  through CHG-193 (PAB plumbing, IAM inline policies,
  workspace quota, scope-authorized callers, etc.) — they
  ride on the same mantad binary.

## Out of scope

CHG-193's own behavioral check (cross-workspace
`PutPublicAccessBlock` against the same bucket name) is not in
this verify suite. The fjall unit tests + the
`mantas3-s3/tests/public_name_gate.rs` SDK harness cover the
gate state machine; a follow-on bead can wire an FDB-driven
SDK harness for the full SDK-driven cross-workspace path.
